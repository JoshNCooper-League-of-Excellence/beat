import fmt::{
  println
};

import lexer::*;
import parser::*;

main :: fn() {
  mut lexer := Lexer::new("test.shela".as_string());

  while {
    if lexer.peek().type == TokenType::Eof {
      break;
    }
    token := lexer.eat().unwrap();
    println(token);
  }
}